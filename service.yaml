apiVersion: v1
kind: Service
metadata:
  name: nodejs-app-svc
  namespace: production
  labels:
    app: nodejs-app
spec:
  type: NodePort
  selector:
    app: nodejs-app
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 3000
      nodePort: 30080
